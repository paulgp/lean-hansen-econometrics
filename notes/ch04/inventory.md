# Chapter 4 theorem inventory

## Target chapter
- `chapters/04-least-squares-regression.pdf`

## Source text
- `notes/ch04/ch4_excerpt.txt` is a text extract of `chapters/04-least-squares-regression.pdf`,
  generated by `uv run python scripts/0_extract_pdf_text.py chapters/04-least-squares-regression.pdf notes/ch04/ch4_excerpt.txt`.

## First-pass formalization order

### Layer 1: deterministic regression algebra
1. state the linear model in finite-sample matrix form, `Y = X β + e`
2. prove Hansen equation (4.6), `β̂ = β + (Xᵀ X)⁻¹ Xᵀ e`
3. prove the orthogonal-error specialization, `Xᵀ e = 0 -> β̂ = β`
4. derive fitted values as signal plus projected error
5. derive residuals as the annihilator applied to the model error

Status: landed in `HansenEconometrics/Chapter4LeastSquaresRegression.lean`.
These are deterministic algebraic statements underlying the expectation proof of Theorem 4.1.

### Layer 2: conditional expectation / unbiasedness
6. formalize the conditional model assumption `E[e | X] = 0`
7. show conditional unbiasedness, `E[β̂ | X] = β`, from equation (4.6)
8. use the law of iterated expectations for unconditional unbiasedness when integrability holds

Status: pending. This should reuse the Chapter 2 conditional-expectation package, but it needs a
clean representation of conditioning on the random design matrix.

### Layer 3: conditional variance
9. define vector covariance and conditional covariance matrices
10. formalize `D = E[e eᵀ | X]`
11. prove Hansen equation (4.10), `var[β̂ | X] = (Xᵀ X)⁻¹ Xᵀ D X (Xᵀ X)⁻¹`
12. specialize to homoskedastic errors, `D = σ² I`, giving `σ² (Xᵀ X)⁻¹`

Status: deterministic matrix core landed in `HansenEconometrics/Chapter4LeastSquaresRegression.lean`:
the `Aᵀ D A` expression and homoskedastic simplification are proved. The stochastic
conditional-covariance bridge is still pending.

### Layer 4: efficiency and covariance estimators
13. Gauss-Markov lower bound among linear unbiased estimators
14. generalized least squares algebra and generalized Gauss-Markov
15. residual variance estimators
16. homoskedastic and heteroskedastic covariance estimators
17. clustered covariance estimator

Status: partially landed in `HansenEconometrics/Chapter4LeastSquaresRegression.lean`.
The deterministic GLS coefficient algebra now exists: definition of `glsBeta`, the decomposition
`β̂_GLS = β + (Xᵀ Ω⁻¹ X)⁻¹ Xᵀ Ω⁻¹ e`, and the weighted-orthogonality specialization.
The positive-semidefinite Gauss-Markov lower-bound proof and the stochastic covariance / expectation
bridge are still pending.

## Immediate target
Continue Chapter 4 with the conditional-unbiasedness bridge: encode the right conditional-mean-zero
assumption for the finite-sample error vector and prove that equation (4.6) implies Theorem 4.1 in
Lean-native conditional-expectation notation, then connect the Chapter 4.2 covariance matrix algebra
to a matrix-valued conditional covariance statement. After that, return to the positive-semidefinite
Gauss-Markov lower-bound proof and the generalized covariance lower bound.
